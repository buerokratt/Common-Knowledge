{{- if .Values.ruuterInternal.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-{{ .Values.ruuterInternal.nameOverride }}
  annotations:
    {{- range $key, $value := .Values.ruuterInternal.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  ingressClassName: "nginx"
  rules:
    - host: {{ .Values.ruuterInternal.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-{{ .Values.ruuterInternal.nameOverride }}
                port:
                  number: {{ .Values.ruuterInternal.service.port }}
  tls:
    - hosts:
        - {{ .Values.ruuterInternal.ingress.host }}
      secretName: {{ .Values.ruuterInternal.ingress.tlsSecret }}
{{- end }}
